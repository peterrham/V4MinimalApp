# Continuous Upload System Architecture

## 📱 User Interface Layer

```
┌─────────────────────────────────────────────────────────┐
│              CameraScanView (Camera UI)                 │
│                                                         │
│  ┌─────┐  ┌─────────────┐  ┌──────────────┐  ┌─────┐ │
│  │  X  │  │ ● Recording │  │ Upload Queue │  │  ⚡ │ │
│  └─────┘  └─────────────┘  │   Badge (3)  │  └─────┘ │
│                             └──────────────┘           │
│                                    │                    │
│                                    │ Tap                │
│                                    ↓                    │
│                      ┌──────────────────────┐          │
│                      │ VideoUploadQueueView │          │
│                      └──────────────────────┘          │
│                                                         │
│              ┌──────────────────────┐                  │
│              │  Record Button       │                  │
│              └──────────────────────┘                  │
└─────────────────────────────────────────────────────────┘
```

## 🎥 Recording Flow

```
User Taps Record Button
         ↓
┌─────────────────────┐
│  CameraManager      │
│  .startRecording()  │
└─────────────────────┘
         ↓
┌──────────────────────────────┐
│ AVCaptureMovieFileOutput     │
│ Records to temp file:        │
│ recording_1234567890.mov     │
└──────────────────────────────┘
         ↓
User Taps Stop Button
         ↓
┌─────────────────────┐
│  CameraManager      │
│  .stopRecording()   │
└─────────────────────┘
         ↓
┌──────────────────────────────────────────┐
│ AVCaptureFileOutputRecordingDelegate     │
│ fileOutput(_:didFinishRecordingTo:...)   │
└──────────────────────────────────────────┘
         ↓
    ┌────┴────┐
    ↓         ↓
 Save to   Add to
 Photos    Queue
```

## 💾 Save to Photos Flow

```
┌─────────────────────────────┐
│ CameraManager               │
│ .saveVideoToLibrary(url)    │
└─────────────────────────────┘
         ↓
┌─────────────────────────────┐
│ Check Photos Permission     │
│ (Request if needed)         │
└─────────────────────────────┘
         ↓
    Permission?
    ├─ Yes ────┐
    └─ No ─────┤
               ↓
         ┌─────────────────┐
         │ PHPhotoLibrary  │
         │ .performChanges │
         └─────────────────┘
               ↓
         ┌─────────────────┐
         │ Video Saved!    │
         │ Show Toast 📱   │
         └─────────────────┘
```

## 📋 Upload Queue System

```
┌──────────────────────────────────────────────────────────────┐
│                    VideoUploadQueue                          │
│                      (Observable)                            │
│                                                              │
│  State:                                                      │
│  • isUploading: Bool                                         │
│  • currentUploadProgress: Double                             │
│  • queuedVideos: [QueuedVideo]                              │
│  • currentUpload: QueuedVideo?                              │
│  • completedUploads: [CompletedUpload]                      │
│  • failedUploads: [FailedUpload]                            │
│                                                              │
│  Configuration:                                              │
│  • autoUpload = true                                         │
│  • deleteAfterUpload = true                                  │
│  • maxRetries = 3                                            │
│  • retryDelay = 5.0s                                         │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Queue Processing Flow

```
Video Added to Queue
         ↓
    ┌────────────┐
    │ Queue Size │
    │  count++   │
    └────────────┘
         ↓
   Auto-Upload?
    ├─ Yes ────┐
    └─ No ─────┘ (Wait for manual start)
               │
               ↓
┌──────────────────────────────┐
│ Start Queue Processing       │
│ while !queuedVideos.isEmpty  │
└──────────────────────────────┘
               ↓
┌──────────────────────────────┐
│ Get Next Video from Queue    │
│ video = queuedVideos.first   │
└──────────────────────────────┘
               ↓
┌──────────────────────────────┐
│ Upload Video (with retry)    │
│ for attempt in 1...3         │
└──────────────────────────────┘
               ↓
          ┌────┴────┐
          ↓         ↓
      Success    Failure
          │         │
          ↓         ↓
    ┌─────────┐ ┌──────────┐
    │Complete │ │  Failed  │
    │  List   │ │   List   │
    └─────────┘ └──────────┘
          │         │
          ↓         │
    Delete File     │
          │         │
          └────┬────┘
               ↓
        Remove from Queue
               ↓
         More Videos?
          ├─ Yes ──→ (Loop back)
          └─ No ───→ Queue Complete
```

## ☁️ Upload to Google Drive Flow

```
┌────────────────────────────┐
│ VideoUploadQueue           │
│ .uploadVideo(video)        │
└────────────────────────────┘
         ↓
┌────────────────────────────┐
│ Read Video File            │
│ Data(contentsOf: url)      │
└────────────────────────────┘
         ↓
┌────────────────────────────┐
│ StreamingVideoUploader     │
│ .startUploadSession()      │
└────────────────────────────┘
         ↓
┌────────────────────────────┐
│ Google Drive API           │
│ POST /upload/drive/v3/files│
│ ?uploadType=resumable      │
└────────────────────────────┘
         ↓
┌────────────────────────────┐
│ Get Session URL            │
│ Location: https://...      │
└────────────────────────────┘
         ↓
┌────────────────────────────┐
│ Upload in 512KB Chunks     │
│ while offset < fileSize    │
└────────────────────────────┘
         ↓
    ┌────────────────┐
    │ Upload Chunk   │
    │ PUT session URL│
    │ Content-Range  │
    └────────────────┘
         ↓
    Update Progress
    (repeat for each chunk)
         ↓
┌────────────────────────────┐
│ Finalize Upload            │
│ PUT bytes */totalSize      │
│ (with retry logic)         │
└────────────────────────────┘
         ↓
    ┌────┴────┐
    ↓         ↓
HTTP 200   Timeout
Success    Retry
    │         │
    ↓         ↓
  Done    Wait & Retry
             (up to 3x)
```

## 🔄 Retry Logic Flow

```
Upload Attempt
      ↓
  Try Upload
      ↓
   Success?
    ├─ Yes ──→ Return Success
    └─ No ───┐
             ↓
      Attempt < Max?
       ├─ Yes ──┐
       └─ No ───┤
                ↓
          Last Attempt?
           ├─ No ───→ Wait (exponential backoff)
           │           ↓
           │      Sleep (2s, 4s, 8s...)
           │           ↓
           │      Retry Upload
           │           ↓
           └───────────┘
                ↓
           Return Failure
```

## 📊 State Management

```
┌──────────────────────────────────────────────────────┐
│                  VideoUploadQueue                    │
│                   @MainActor                         │
│                 @ObservableObject                    │
└──────────────────────────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        ↓              ↓              ↓
┌──────────────┐ ┌──────────┐ ┌─────────────┐
│@Published    │ │@Published│ │  @Published │
│queuedVideos  │ │isUploading│ │ progress   │
└──────────────┘ └──────────┘ └─────────────┘
        │              │              │
        ↓              ↓              ↓
┌──────────────────────────────────────────┐
│        SwiftUI Views Auto-Update          │
│                                           │
│  • UploadQueueBadge (badge count)        │
│  • VideoUploadQueueView (full list)      │
│  • CurrentUploadRow (progress bar)       │
└──────────────────────────────────────────┘
```

## 🔔 Notification Flow

```
Upload Events
      │
      ├─ Video Added ──→ (Internal state change)
      │
      ├─ Upload Complete ──┐
      │                    ↓
      │         ┌─────────────────────────┐
      │         │ NotificationCenter.post │
      │         │ "VideoUploadCompleted"  │
      │         └─────────────────────────┘
      │                    ↓
      │              Observers Notified
      │              (UI updates, logs)
      │
      ├─ Upload Failed ────┐
      │                    ↓
      │         ┌─────────────────────────┐
      │         │ NotificationCenter.post │
      │         │ "VideoUploadFailed"     │
      │         └─────────────────────────┘
      │
      └─ Queue Complete ───┐
                           ↓
                ┌─────────────────────────┐
                │ NotificationCenter.post │
                │ "VideoUploadQueueComplete"│
                └─────────────────────────┘
```

## 🎯 Complete User Journey

```
User Opens Camera
      ↓
Start Recording
      ↓
Record Video (30 seconds)
      ↓
Stop Recording
      ↓
      ├──→ Save to Photos Library ✅
      │         (User can access immediately)
      │
      └──→ Add to Upload Queue
                ↓
          Queue Badge Shows "1"
                ↓
          Upload Starts Automatically
                ↓
          Progress: 0% → 100%
                ↓
          Upload Complete ✅
                ↓
          Delete Local File
                ↓
          Queue Badge Shows "0"
                
User Records 2 More Videos
      ↓
Queue Badge Shows "2"
      ↓
Upload #1 starts (progress bar)
      ↓
Upload #1 completes
      ↓
Upload #2 starts (progress bar)
      ↓
Upload #2 completes
      ↓
Queue Empty ✅

User Taps Queue Badge
      ↓
See Full Queue View:
  • Currently Uploading: video_3.mov (45%)
  • Queue (2): video_4.mov, video_5.mov
  • Completed (3): video_1, video_2, video_3
  • Failed (0): -
      ↓
User Can:
  • Pause/Resume uploads
  • Remove videos from queue
  • Retry failed uploads
  • Toggle settings
  • Clear history
```

## 🏗️ Component Relationships

```
┌────────────────────────────────────────────────────┐
│                 CameraScanView                     │
│  ┌──────────────┐  ┌──────────────────────────┐  │
│  │CameraManager │  │UploadQueueBadge          │  │
│  │   (Camera)   │  │(Shows queue status)      │  │
│  └──────────────┘  └──────────────────────────┘  │
│         │                      │                   │
│         │                      ↓                   │
│         │          ┌───────────────────────────┐  │
│         │          │ VideoUploadQueueView      │  │
│         │          │ (Full queue management)   │  │
│         │          └───────────────────────────┘  │
│         ↓                      │                   │
│  ┌──────────────┐              │                  │
│  │Video Recorded│              │                  │
│  └──────────────┘              │                  │
│         │                      │                   │
│         ↓                      ↓                   │
│  ┌────────────────────────────────────────────┐  │
│  │       VideoUploadQueue.shared              │  │
│  │         (Singleton)                        │  │
│  └────────────────────────────────────────────┘  │
│                   │                               │
│                   ↓                               │
│  ┌────────────────────────────────────────────┐  │
│  │     StreamingVideoUploader                 │  │
│  │     (Handles Google Drive API)             │  │
│  └────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

This architecture provides a robust, user-friendly system for continuous video uploads with full error handling, progress tracking, and manual control when needed! 🎥☁️✨
